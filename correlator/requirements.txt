# Correlator (worker pool) — fuses the observation bus and runs the video verifier.
# Subscribes fe-observations; publishes fe-incidents + the verifier's video reads;
# writes incidents/ + agent_status/ to Firestore; reads gs:// mosaics and calls
# Vertex Gemini (verifier + report narrative).
pydantic>=2.5,<3
google-cloud-pubsub>=2.25
google-cloud-firestore>=2.19
google-cloud-storage>=2.18
google-genai>=0.8
