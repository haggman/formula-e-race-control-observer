# Formula E — Proactive Race Control Observer — Python dependencies
#
# Installed into .venv/ by ./activate. Minimum-version floors unless a strict
# pin is needed for protocol/API surface.

# --- Data contracts + telemetry detector (deterministic core) ---
pydantic>=2.5,<3
pandas>=2.1
pyarrow>=15.0

# --- Data plane (streams → Pub/Sub → Firestore) ---
google-cloud-pubsub>=2.25
google-cloud-firestore>=2.19
google-cloud-storage>=2.18

# --- Agents (ADK + Vertex; A2A between observers and correlator) ---
# Strict pin: ADK agent definition + A2A surface changed across 1.x.
google-adk>=1.0,<2
google-cloud-aiplatform[adk,agent_engines]>=1.70

# --- Video observer (Gemini Live API, 1 FPS multimodal) ---
google-genai>=0.8
# Static ffmpeg binary (no system apt-get needed) for mosaic frame extraction.
imageio-ffmpeg>=0.5

# --- BigQuery (race-control corroboration, history) ---
google-cloud-bigquery>=3.27

# --- Frontend (Race Control console + websocket) ---
fastapi>=0.115
uvicorn[standard]>=0.32
httpx>=0.27

# --- Utilities ---
python-dotenv>=1.0
