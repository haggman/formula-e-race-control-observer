"""solution — the complete reference answer key (parallel to starter/).

Holds the finished Video Verifier the demo/instructor build runs. Select it with
    export VERIFIER_PACKAGE=solution.video_verifier
(the deployed/demo path defaults to solution; local dev defaults to starter).
"""
