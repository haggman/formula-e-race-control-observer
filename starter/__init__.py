"""starter — parallel package holding ONLY the student's component.

The Video Verifier you build lives in starter/video_verifier/. Everything else in
the repo (correlator, telemetry observer, frontend, setup, shared…) is shared with
solution/ and lives at the top level. The correlator loads the verifier from
whichever package $VERIFIER_PACKAGE names (default: starter.video_verifier) — see
shared/verifier_pkg.py.
"""
